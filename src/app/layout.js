import "./globals.css";

export const metadata = {
  title: "Tahseel Academy",
  description: "Courses",
  icons: {
    icon: "./favicon.ico",
  },
};

export default function RootLayout({ children }) {
  return children;
}
